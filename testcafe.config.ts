const config = {
    src: 'tests/**/*.ts', // Path to your test files
    browsers: ['chrome:headless'], // Specify the browsers you want to use
    reporter: 'spec', // Specify the reporter you want to use
    // Other options can be added here
};

export default config;